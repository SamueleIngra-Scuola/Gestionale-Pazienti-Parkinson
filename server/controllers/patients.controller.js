const patients = require("../../imports/api/v1/patients");
// @ts-ignore
import { Request, Response } from "express";

/**
 * 
 * @param {Request} req 
 * @param {Response} res 
 */
export var CreatePatientAccount = (req, res) => {

}

module.exports = {
    CreatePatientAccount,
}
    
